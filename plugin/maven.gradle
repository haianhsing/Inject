apply plugin: 'maven'

//发布在本地，../plugin 文件夹
uploadArchives {
    repositories.mavenDeployer {
        repository(url: uri('../plugins'))
        pom.groupId = 'com.unite.plugin'
        pom.artifactId = 'InjectRandomCode'
        pom.version = '1.0.0'
    }
}